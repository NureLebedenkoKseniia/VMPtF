Практичне зайняття 2

Лебеденко Ксенія
ПЗПІ-23-9

1.6. Зміна кольору тексту: Створіть файл HTML з елементом <script> та кнопкою. Використовуйте fetch для завантаження JSON-файлу з кольором (наприклад, #FF0000). Розберіть JSON-файл за допомогою JSON.parse(). Змініть колір тексту кнопки на основі отриманого кольору.

2.1. Парсер JSON: Напишіть програму, яка приймає JSON-рядок в якості вхідних даних і розбиває його на окремі елементи (об'єкти, масиви, значення).

• Використовуйте вбудовані функції мови програмування для роботи з JSON.

• Виведіть результат у зручному форматі (наприклад, деревоподібна структура, таблиця).

3.6. Створіть інтерактивну карту, де користувачі можуть додавати маркери для подій (наприклад, концертів, фестивалів, виставок тощо).

4.6. Для кожного елемента у вашому списку додайте можливість переходу на окрему сторінку, де будуть відображені деталі цього елемента.
